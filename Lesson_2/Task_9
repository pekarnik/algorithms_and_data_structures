# Среди натуральных чисел, которые были введены, найти наибольшее по сумме цифр. Вывести на экран это число и сумму его
# цифр.

def sum_digs(num):
    sum = 0
    while num!=0:
        sum += num % 10
        num = num // 10
    return  sum


n = int(input('Введите натуральное число или 0 для окончания'))
temp = 0
res = n
while n != 0:
    if temp < sum_digs(n):
        temp = sum_digs(n)
        res = n
    else:
        n = int(input('Введите натуральное число или 0 для окончания'))
print(f'Число {res}, сумма {temp}')